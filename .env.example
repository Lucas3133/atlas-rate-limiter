# ================================================================
# ATLAS RATE LIMITER - CONFIGURAÇÃO
# ================================================================

# Redis (Upstash)
UPSTASH_REDIS_URL=redis://default:seu_token_aqui@exemplo.upstash.io:6379

# Rate Limit - Token Bucket
# FIX-001: Refill rate baixo previne ataques contínuos
RATE_LIMIT_CAPACITY=100
RATE_LIMIT_REFILL_RATE=1

# Infraestrutura
REDIS_TIMEOUT_MS=2000
NODE_ENV=development

# API
PORT=3000

# ================================================================
# FIX-002: SEGURANÇA - TRUST PROXY
# ================================================================
# Valores:
#   false ou 0 = Não confia em proxy (local dev) - PADRÃO SEGURO
#   1          = Primeiro proxy (Railway, Render, Vercel, Heroku)
#   true       = Qualquer proxy (Cloudflare, múltiplos proxies)
#
# ⚠️ IMPORTANTE: Só ative se estiver atrás de proxy real!
# ================================================================
TRUST_PROXY=false

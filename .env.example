# ================================================================
# ATLAS RATE LIMITER - CONFIGURATION
# ================================================================

# Redis (Upstash)
UPSTASH_REDIS_URL=redis://default:your_token_here@example.upstash.io:6379

# Rate Limit - Token Bucket
# FIX-001: Low refill rate prevents continuous attacks
RATE_LIMIT_CAPACITY=100
RATE_LIMIT_REFILL_RATE=1

# Infrastructure
REDIS_TIMEOUT_MS=2000
NODE_ENV=development

# API
PORT=3000

# ================================================================
# FIX-002: SECURITY - TRUST PROXY
# ================================================================
# Values:
#   false or 0 = Trust no proxy (local dev) - SECURE DEFAULT
#   1          = First proxy (Railway, Render, Vercel, Heroku)
#   true       = Any proxy (Cloudflare, multiple proxies)
#
# ⚠️ IMPORTANT: Only enable if you're behind a real proxy!
# ================================================================
TRUST_PROXY=false
